/* --- used to collapse rows in the mission log generator --- */
/* --- kinda gave up at some point  ¯\_(ツ)_/¯ --- */

/*
$(".gen_category_title").on('click', function(event) {
    if ($(".gen_general").height = "fit-content") {
        $(".gen_general").height("0px")
    } else {
        $(".gen_general").height("fit-content")
    }

});
*/